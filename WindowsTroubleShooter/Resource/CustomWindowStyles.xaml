<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="CaptionButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="#dddddd"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Width" Value="45"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" Name="PART_ButtonBorder">
                        <TextBlock Text="{TemplateBinding Content}"
                                   FontFamily="Segoe Fluent Icons"
                                   FontSize="10"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"
                                   RenderOptions.EdgeMode="Aliased" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_ButtonBorder" Property="Background" Value="#3F3F3F"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_ButtonBorder" Property="Background" Value="#5A5A5A"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#666666"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CloseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource CaptionButtonStyle}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E81123"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#F1707A"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="CustomWindowStyle" TargetType="{x:Type Window}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#dddddd"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="AllowsTransparency" Value="False"/>
        <Setter Property="WindowStyle" Value="SingleBorderWindow"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"
                        x:Name="WindowBorder">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid x:Name="PART_TitleBar"
                              Grid.Row="0"
                              Height="30"  Background="#202020" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="{Binding Icon, RelativeSource={RelativeSource TemplatedParent}}"
                                   Height="16" Width="16" Margin="5,0,0,0" VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="1"
                                       Text="{TemplateBinding Title}"
                                       Margin="10,0,0,0"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontWeight="SemiBold"/>

                                <StackPanel Grid.Column="2" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="MinimizeButton"
                                        Grid.Column="2"
                                        Content="&#xE921;"  ToolTip="Minimize"
                                        Command="{Binding Source={x:Static SystemCommands.MinimizeWindowCommand}}"
                                        Style="{StaticResource CaptionButtonStyle}" />
                                    <Button x:Name="MaximizeRestoreButton"
                                        Grid.Column="3" ToolTip="Maximize"
                                        Command="{Binding Source={x:Static SystemCommands.MaximizeWindowCommand}}"
                                        Style="{StaticResource CaptionButtonStyle}">
                                        <Button.Content>
                                            <MultiBinding Converter="{StaticResource WindowStateToIconConverter}">
                                                <Binding Path="WindowState" RelativeSource="{RelativeSource TemplatedParent}"/>
                                            </MultiBinding>
                                        </Button.Content>
                                    </Button>
                                    <Button x:Name="CloseButton"
                                        Grid.Column="4" ToolTip="Close"
                                        Content="&#xE8BB;"
                                        Command="{Binding Source={x:Static SystemCommands.CloseWindowCommand}}"
                                        Style="{StaticResource CloseButtonStyle}" />
                                </StackPanel>
                            </Grid>

                            <Border Grid.Row="1" Background="#202020">
                                <AdornerDecorator>
                                    <ContentPresenter Content="{TemplateBinding Content}" />
                                </AdornerDecorator>
                            </Border>

                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="MaximizeRestoreButton" Property="Content" Value="&#xE923;" />
                            <Setter TargetName="MaximizeRestoreButton" Property="ToolTip" Value="Restore Down"/>
                            <Setter TargetName="MaximizeRestoreButton" Property="Command" Value="{Binding Source={x:Static SystemCommands.RestoreWindowCommand}}"/>
                        </Trigger>
                        <Trigger Property="WindowState" Value="Normal">
                            <Setter TargetName="MaximizeRestoreButton" Property="Content" Value="&#xE922;" />
                            <Setter TargetName="MaximizeRestoreButton" Property="ToolTip" Value="Maximize"/>
                            <Setter TargetName="MaximizeRestoreButton" Property="Command" Value="{Binding Source={x:Static SystemCommands.MaximizeWindowCommand}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>