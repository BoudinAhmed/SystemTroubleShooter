<Window x:Class="SystemTroubleShooter.View.SoundDevicesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:vm="clr-namespace:SystemTroubleShooter.ViewModel"
        mc:Ignorable="d" 
        Height="750" Width="700"
        Background="#202020"
        Foreground="#dddddd"
        FontFamily="Segoe UI">
    <Window.DataContext>
        <vm:SoundDevicesViewModel/>
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,30">
            <Button Command="{Binding LoadOutputDevicesCommand}"
                    Tag="Output" Width="170" Height="170" Margin="15" Background="#2b2b2b" Style="{StaticResource ButtonStyle}">
                <StackPanel>
                    <TextBlock Text="&#xE7F6;" FontFamily="Segoe MDL2 Assets" FontSize="80" Foreground="#eee" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="Output Devices" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding LoadInputDevicesCommand}"
                    Tag="Input" Width="170" Height="170" Margin="15"  Background="#2b2b2b" Style="{StaticResource ButtonStyle}">
                <StackPanel>
                    <TextBlock Text="&#xE720;" FontFamily="Segoe MDL2 Assets" FontSize="80" FontWeight="Light" Foreground="#eee" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="Input Devices" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Devices}"
                  Background="#202020" BorderThickness="0" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  HorizontalContentAlignment="Stretch" 
                  Margin="50,0,50,10">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0,3"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#dddddd"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="ItemBorder"
                                        Background="#2b2b2b" BorderThickness="0" CornerRadius="4" Padding="12,10">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="#4A4A4A"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="#007ACC"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding}" VerticalAlignment="Center" FontSize="14" Margin="0,0,5,0"/>

                        
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
