<Window x:Class="SystemTroubleShooter.View.ResultWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystemTroubleShooter.View"
        mc:Ignorable="d"
         Title="Result" Height="350" Width="500" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI">

    <Border CornerRadius="10" Background="#2b2b2b" BorderBrush="#444444" BorderThickness="1"> <Grid Margin="20"> <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>    <RowDefinition Height="*"/>      <RowDefinition Height="Auto"/>    </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0 0 0 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="*"/>  </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="&#xE73E;"  FontFamily="Segoe MDL2 Assets"
                           Foreground="#4CAF50" FontSize="30" VerticalAlignment="Center"
                           Margin="0,0,15,0"/> <TextBlock Grid.Column="1"
                           Text="Success"  Foreground="#EEEEEE"
                           FontSize="24" FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           TextWrapping="Wrap"/>
                </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                 <TextBlock TextWrapping="Wrap"
                           Foreground="#bbbbbb"
                           FontSize="14"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Left"
                           LineHeight="20">
                           This is the default message text that provides details about the troubleshooting result.

                           If it was successful, it explains what was fixed.
                           If it failed, it explains why and provides details about the errors encountered during the process.

                           This text should be bound to the ViewModel's Message property.

                           </TextBlock>
            </ScrollViewer>

            <Button Grid.Row="2"
                    Content="Close"
                    Width="100" Height="35" Margin="0 20 0 0" HorizontalAlignment="Right"
                    Background="#3c3c3c"
                    Foreground="#dddddd"
                    BorderBrush="#555555"
                    BorderThickness="1"
                    Cursor="Hand">
                 </Button>

        </Grid>
    </Border>
</Window>

